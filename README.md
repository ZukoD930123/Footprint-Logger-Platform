# Footprint Logger Platform

## How to run locally:
1. Clone the repo: `git clone [YOUR_URL]`
2. Install dependencies for both folders:
   - `cd client && npm install`
   - `cd ../server && npm install`
3. Start the server: `node server.js` (in /server)
4. Start the app: `npm start` (in /client)
